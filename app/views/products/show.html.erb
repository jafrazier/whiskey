<div class="productContainer">
<%= render 'layouts/nav' %>
<div class="popup">
  <div class="reg">
    <%= render 'devise/registrations/form'%>
  </div>
  <div class="log">
    <%= render 'devise/sessions/form'%>
  </div>
</div>
<div class="productShow">
  <h2><%= @product.brand %> - <%= @product.name %>  <%if current_user && admin? %><%= link_to 'Edit', edit_product_path(@product) %>  | <%= link_to 'Destroy', @product , method: :delete, data: { confirm: 'Are you sure?' } %> <%end%></h2>
<p id="notice"><%= notice %></p>
<%= image_tag(@product.image) %>
<div class="productInfo">
<p>
  <strong>Type:</strong>
  <%= @product.type_name %>
</p>
<p>
  <strong>Alcochol by volume:</strong>
  <%= @product.abv %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>
<p>
  <strong>More Info:</strong>
  <%= @product.more_info %>
</p>
<button type="button" name="button" class="buy" id="buy"><a href="'https://www.google.com/search?q=<%=@product.brand%>%20<%=@product.name%>&source=lnms&tbm=shop'" target="_blank">Buy on Google</a></button>
</div>
<hr class="break">
<%if current_user %>
<div class="commentForm"><h3>leave a review</h3><%= render partial: 'comments/form' %></div>
<%else%>
<p>Sign up to leave a review</p>
<%end%>
<div class="commentContent" >
  <h3>Reviews(<%= @product.comments.size %>)</h3>
  <%= render partial: 'products/comment', collection: @product.comments.reverse %>
</div>


</div>
</div>
