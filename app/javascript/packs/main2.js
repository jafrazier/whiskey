import Vue from 'vue/dist/vue.esm'

document.addEventListener('DOMContentLoaded', () => {
  const test = new Vue({
    el: '#vuetest',
    data: {
      message: "finally"
    }
  })
  const buy = new Vue({
    el: "#buy",
    data: {
      googleBuy:
    }

  })

})
